# Preflight per-project config.
# Generated by: wire-siblings/mdreader.sh
# Stack: Swift/Xcode macOS native (MDReader.xcodeproj, XcodeGen project.yml).
# No JS runtime present. preflight's web-focused jobs do not apply here.

[project]
# skip=true: preflight's fast/heavy tiers target JS/TS runtimes.
# Swift builds run via xcodebuild outside preflight's scope.
# This silences the "no supported runtime detected" warning on pre-push.
skip = true
